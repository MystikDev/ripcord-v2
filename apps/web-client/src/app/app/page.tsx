'use client';

/**
 * Default app page. The AppShell already renders the full layout,
 * so this is essentially a no-op page that fills the /app route.
 */
export default function AppPage() {
  // The AppShell in the layout handles rendering the 3-column view.
  // This page is intentionally empty as the layout provides the full UI.
  return null;
}
