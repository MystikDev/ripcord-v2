# Copy to .env.local for local development.
# In production, these are baked into the bundle at docker build time
# via the WEB_PUBLIC_* args in docker-compose.yml.

NEXT_PUBLIC_API_URL=https://api.ripcord.gg
NEXT_PUBLIC_AUTH_URL=https://auth.ripcord.gg
NEXT_PUBLIC_GATEWAY_URL=wss://gw.ripcord.gg
